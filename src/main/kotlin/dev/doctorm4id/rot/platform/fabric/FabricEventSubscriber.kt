package dev.doctorm4id.rot.platform.fabric

//? fabric {

/*import dev.doctorm4id.rot.event.ExampleEventHandler // sample_content
import net.fabricmc.fabric.api.entity.event.v1.ServerLivingEntityEvents // sample_content
import net.minecraft.server.level.ServerPlayer // sample_content

class FabricEventSubscriber {

	fun registerEvents() {
		//? != 1.20.1 {
		// sample_content
		ServerLivingEntityEvents.AFTER_DAMAGE.register(AfterDamage { entity: LivingEntity?, source: DamageSource?, baseDamage: Float, damageTaken: Float, blocked: Boolean ->  // sample_content
			if (entity is ServerPlayer && damageTaken > 0) { // sample_content
				ExampleEventHandler().onPlayerHurt(entity) // sample_content
			} // sample_content
		}) // sample_content
		//?}
	}
}

*///? }
